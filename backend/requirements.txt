# API server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Data validation (FastAPI brings pydantic transitively, but pinning explicitly)
pydantic>=2.0

# File upload parsing (if needed in future)
python-multipart>=0.0.6

# Export helpers
python-docx>=1.1.0
weasyprint>=60.0

# Note: WeasyPrint requires system libraries (Cairo, Pango, GDK-Pixbuf, libffi).
# On Windows, consider installing via WSL or follow WeasyPrint Windows instructions.
